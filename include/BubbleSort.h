#pragma once

namespace chain {
    class BubbleSort{
    public: int dist1,dist2,dist32;
    public: void static Sort(int &dist1, int &dist2, int &dist3);
    public: virtual ~BubbleSort();
    };
}

